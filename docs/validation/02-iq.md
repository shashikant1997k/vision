# 02 — Installation Qualification (IQ)

Verifies the system and its prerequisites are installed correctly on the line PC.
Execute per installation; record actual values and Pass/Fail with initials + date.

## A. Hardware prerequisites

| # | Item | Requirement | Actual | P/F |
|---|---|---|---|---|
| A1 | Line PC | Windows 10/11 x64 (or Linux x64); CPU ≥ 8 cores for typical config (≥12 for max), ≥16 GB RAM | | |
| A2 | GigE NIC | Dedicated NIC; jumbo frames (MTU 9014) enabled; RX buffers raised; interrupt moderation tuned for latency ([D-011](../decisions/decision-log.md)) | | |
| A3 | Camera | GigE Vision / GenICam camera; firmware recorded | | |
| A4 | GenTL producer | Vendor `.cti` installed; path set via `VIS_GENTL_CTI` | | |
| A5 | Reject I/O | Digital output / Modbus TCP block reachable; lane→channel map recorded | | |
| A6 | Lighting | Strobe/continuous controller wired; channels recorded | | |
| A7 | Time source | NTP configured; clock synced (audit timestamps) | | |

## B. Software prerequisites

| # | Item | Requirement | Actual | P/F |
|---|---|---|---|---|
| B1 | Python | 3.11+ (recorded `python --version`) | | |
| B2 | PostgreSQL | 14+ installed and running; database created | | |
| B3 | DB driver | `psycopg` installed (`.[postgres]` extra) | | |
| B4 | Application | Installed (MSI / `pip install`); version + git commit recorded | | |
| B5 | Optional modules | `.[codes]`, `.[ocr]`, `.[camera]`, `.[io]`, `.[hmi]` installed as licensed | | |

## C. Installation & configuration

| # | Step | Verification | Actual | P/F |
|---|---|---|---|---|
| C1 | Set `DATABASE_URL` to the PostgreSQL DSN | App connects | | |
| C2 | Apply DB schema (Alembic migrations) | All tables created; `alembic current` = head | | |
| C3 | **Lock audit table** | `REVOKE UPDATE, DELETE ON audit_entries FROM <app_role>` executed | | |
| C4 | Create the initial admin user + roles | Login succeeds; roles present | | |
| C5 | Configure the station (cameras, reject lanes, lighting) | Records present + audited | | |
| C6 | Pin the OCR/AI model version | `model_versions` row present; sha256 recorded ([D-007]) | | |
| C7 | Image archive path + retention policy set | Path writable; policy recorded | | |

## D. Installed-version & integrity record

| Component | Version / hash | Recorded by |
|---|---|---|
| Application (git commit) | | |
| Python | | |
| PostgreSQL | | |
| ONNX Runtime / OCR model | | |
| GenTL producer (.cti) | | |

## E. Build-info / smoke check

| # | Step | Expected | P/F |
|---|---|---|---|
| E1 | Launch HMI (`vis-hmi`) | Login window appears | |
| E2 | Log in as admin | Live screen opens; user shown in status bar | |
| E3 | Run automated suite (`pytest -q`) | All tests Pass (record count) | |

**IQ executed by:** ____________  **Reviewed (QA):** ____________  **Date:** ______
